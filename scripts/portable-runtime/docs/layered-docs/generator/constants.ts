import {
  type BehaviorFoundationType,
  type PrimitiveDocsFrameworkTarget,
  type StyledFrameworkTarget,
  type StylingRecipeMetadata,
  type StylingSectionMetadata,
} from "../types.js";

export const FRAMEWORK_TARGETS = [
  "astro",
  "react",
] as const satisfies readonly StyledFrameworkTarget[];

export const PRIMITIVE_DOCS_FRAMEWORK_TARGETS = [
  "raw-html",
  "astro",
  "react",
  "solid",
  "svelte",
  "vue",
] as const satisfies readonly PrimitiveDocsFrameworkTarget[];

export const PRIMITIVE_ANATOMY_FRAMEWORK_ORDER = [
  "astro",
  "react",
  "raw-html",
] as const satisfies readonly PrimitiveDocsFrameworkTarget[];

export const RUNTIME_DOCS_PATH = "/docs/runtime/";

export const STYLING_DOCS_PATH = "/docs/styling/";

export const GENERATED_MDX_MARKER_TEXT =
  "Generated by scripts/portable-runtime/generate-layered-docs-metadata.ts.";

export const GENERATED_MDX_COMMENT = `{/* ${GENERATED_MDX_MARKER_TEXT} Do not edit directly. */}`;

export const RUNTIME_INITIALIZER_FACTORY_ALIASES = {
  initSidebarController: "createSidebarController",
} as const satisfies Record<string, string>;

export const FOUNDATION_LABELS = {
  "direct-primitive": "Direct primitive",
  "renamed-primitive": "Renamed primitive",
  composite: "Composite",
  "mixed-conditional": "Mixed conditional",
  "styled-only": "Styled only",
} as const satisfies Record<BehaviorFoundationType, string>;

export const STYLING_SECTIONS = [
  { id: "theme-tokens", title: "Theme Tokens" },
  { id: "component-variants", title: "Component Variants" },
  { id: "state-styling", title: "State Styling" },
  { id: "slots-anatomy", title: "Slots And Anatomy" },
  { id: "framework-style-layers", title: "Framework Style Layers" },
  { id: "customization-recipes", title: "Customization Recipes" },
] as const satisfies readonly Omit<StylingSectionMetadata, "docsPath">[];

export const STYLING_RECIPES = [
  {
    id: "class-overrides",
    title: "Class Overrides",
    summary:
      "Pass class or className to adjust a rendered styled component while keeping its contract and Runtime hooks intact.",
  },
  {
    id: "variant-extension",
    title: "Variant Extension",
    summary:
      "Extend a variant collection when the component exposes public variant props and the customization belongs to every use of that variant.",
  },
  {
    id: "styled-wrapper-edits",
    title: "Styled Wrapper Edits",
    summary:
      "Edit or fork the styled wrapper when markup, icons, slots, or local CSS need to change but the same primitive contract should remain underneath.",
  },
  {
    id: "primitive-composition",
    title: "Primitive Composition",
    summary:
      "Compose primitives directly when the styled layer is missing, renamed, or too opinionated for the behavior you need.",
  },
] as const satisfies readonly StylingRecipeMetadata[];
