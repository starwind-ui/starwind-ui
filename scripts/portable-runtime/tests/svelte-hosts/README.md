# Packed Svelte CLI hosts

Run `pnpm svelte:hosts --host=vite --output=/absolute/path/report.json` from the repository root. The command builds and packs Runtime, Svelte, and the Starwind CLI, then installs the exact archives into an owned temporary consumer. It requires registry access and Chrome. Use `--host=sveltekit` for the request-server fixture. Use `--host=astro` for the Svelte island fixture. Use `pnpm test:svelte-cli-host-acceptance` for one pack set across all three hosts, or pass `--packs=/absolute/path` to reuse a directory containing the exact three tarballs.

The public path resolves the consumer-local CLI package and binary, then runs installed `init --defaults --framework svelte` and `add` for the five Styled groups. The Vite fixture keeps its existing vendored Select witness. Its production preview runs three mount cycles that check Checkbox cancellation and accepted output, composed Button triggers, native Dialog ownership, Select acceptance, Theme Toggle, callback refs, and stale detached owners. Browser warnings, errors, and failed requests fail the run.

Evidence includes archive SHA-256 values, the hashes of packed files, installed package versions and paths, the full resolved dependency tree and lockfile, production module paths, host tool entries, and each browser cycle. The isolated pnpm overrides bind each packed package throughout the dependency tree. Installed package files must equal the packed payload. Each bundled Starwind file must belong to the exact verified package directory, so another installation with the same version fails. Each build module must resolve inside the consumer; Starwind imports must use dist. The runner closes its server and removes its temporary consumer before writing evidence. The default report directory is separate and remains available after cleanup.

The ordinary `portable-runtime` unit project discovers the runner, SvelteKit, and Astro unit tests. Its injected stages test failures without registry work. The explicit host command runs the network and browser proof.

The fixture uses the documented [Vite build and preview APIs](https://vite.dev/guide/api-javascript.html) and [Svelte mount and unmount APIs](https://svelte.dev/docs/svelte/imperative-component-api). Pins follow the approved slice spec.

The SvelteKit fixture pins Kit 2.70.3 with adapter-node 5.5.7 and uses the same Svelte 5.29.0, Vite 7.3.5, and plugin-svelte 6.2.1 pins. Its [Node adapter custom server](https://svelte.dev/docs/kit/adapter-node#Custom-server) serves the production handler on an owned ephemeral port. [Page options](https://svelte.dev/docs/kit/page-options) enable request SSR and hydration, with prerendering disabled. Both production build graphs must use the verified packed directories. Starwind dependencies are bundled in the SSR build so their actual module paths can be checked.

The SvelteKit browser check requests the controls with JavaScript disabled and checks their tags, accepted defaults, and content in server HTML. It then checks hydration and two client-link navigation cycles. Each cycle exercises accepted models, Checkbox cancellation, the Button child trigger, callback-ref replacement, Select, Theme Toggle, and Dialog focus return. Navigation leaves an open Dialog and checks removal of its old nodes, balanced ref cleanup, release of scroll locking, and inert stale elements. A document token proves that links use client navigation. Return navigation must create a fresh owner with working controls. Browser diagnostics fail the run. Fixture routes are confined to the isolated host consumer.

The Astro fixture pins Astro 7.1.3 and `@astrojs/svelte` 9.0.1 with the approved Svelte 5.57.0 comparison version. Its compatible tools are Vite 8.1.3, plugin-svelte 7.3.0, and TypeScript 5.9.3. The integration metadata requires Vite 8 and Svelte >=5.43.6. Vite and SvelteKit keep their Svelte 5.29.0 floor. The Astro host records the integration's ESM plugin and Vite resolution, the plugin's ESM compiler resolution, and Astro's Vite resolution. Each must match the installed host entry. A bounded Node child uses [`import.meta.resolve` with its parent URL enabled](https://nodejs.org/api/esm.html#importmetaresolvespecifier) to follow import conditions. A focused test covers distinct ESM/CommonJS compiler exports and a decoy compiler beside the importer.

Astro's [Svelte integration](https://docs.astro.build/en/guides/integrations-guide/svelte/) renders one island with `client:load`. The complete compound tree, callbacks, and snippets stay inside Svelte. A navigation URL string is the only prop passed from Astro. Both pages use the documented [`ClientRouter`](https://docs.astro.build/en/guides/view-transitions/#enabling-view-transitions-spa-mode). The [programmatic build and preview APIs](https://docs.astro.build/en/reference/programmatic-reference/) serve generated SSR HTML and production assets from the isolated consumer. Both build graphs must use the verified packed directories. The shared navigation proof checks server controls, hydration, model transactions, focus, ref balance, stale callbacks, and a fresh owner after return. Astro also checks one SSR island with `client:load`, completion of hydration, removal of the old island, and absence of islands on the destination page.

The code names this preserved workload `REPRESENTATIVE_SVELTE_STYLED_ROOTS`: Button, Checkbox,
Select, Dialog and Theme Toggle. Each report records this scope beside the current generated Primitive and Styled inventories. Package payload accounting includes the complete
current package; workload timings and host behavior cover only the representative cohort.

The retained source-command seam covers repeat init, update, removal, discovery, and vendoring without installing host packages. Public acceptance does not repeat that lifecycle. Both paths use the same fixtures, production builds, browser checks, provenance checks, server cleanup, and temporary-directory cleanup.
