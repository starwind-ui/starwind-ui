name: Published Release Acceptance

on:
  workflow_dispatch:
    inputs:
      version:
        description: Exact published Starwind CLI version (for example, 3.0.0-beta.2 or 3.0.0)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: published-release-${{ inputs.version }}
  cancel-in-progress: false

jobs:
  acceptance:
    name: Astro and React published-package acceptance
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout Repo
        # checkout@6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Install pnpm
        # pnpm/action-setup@v6.0.8
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
        with:
          cache: true

      - name: Setup Node.js 24
        # actions/setup-node@v6.4.0
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 24
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Chromium
        run: pnpm --filter=react-demo exec playwright install --with-deps chromium

      - name: Test Published Release
        run: pnpm test:published-release -- --version "${{ inputs.version }}" --artifacts .artifacts/published-release

      - name: Upload Acceptance Diagnostics
        if: always()
        # upload-artifact@v6
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: published-release-${{ inputs.version }}
          path: .artifacts/published-release
          if-no-files-found: ignore
          retention-days: 7
