<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Svelte proof output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script lang="ts">
  import { type DialogCloseCompleteDetails, type DialogOpenChangeDetails, createDialog } from "@starwind-ui/runtime/dialog";
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";

  type Props = Omit<HTMLAttributes<HTMLDivElement>, "children"> & {
    children?: Snippet<[boolean]>;
    defaultOpen?: boolean;
    open?: boolean;
    closeOnEscape?: boolean;
    closeOnOutsideInteract?: boolean;
    modal?: boolean;
    onOpenChange?: (open: boolean, detail: DialogOpenChangeDetails) => void;
    onCloseComplete?: (detail: DialogCloseCompleteDetails) => void;
    ref?: (element: HTMLDivElement | null) => void;
  };

  let { children, defaultOpen = false, open = $bindable(), closeOnEscape = true, closeOnOutsideInteract = true, modal = true, onOpenChange, onCloseComplete, ref, ...rest }: Props = $props();
  const externallyControlled = untrack(() => open !== undefined);
  const initialDefaultOpen = untrack(() => defaultOpen);
  let uncontrolledOpen = $state(initialDefaultOpen);
  let renderedOpen = $derived(open !== undefined ? open : uncontrolledOpen);

  function handleOpenChange(nextOpen: boolean, detail: DialogOpenChangeDetails): void {
    onOpenChange?.(nextOpen, detail);
    if (detail.isCanceled) return;
    if (!externallyControlled) uncontrolledOpen = nextOpen;
    open = nextOpen;
  }

  function handleCloseComplete(detail: DialogCloseCompleteDetails): void {
    onCloseComplete?.(detail);
  }

  const attachRuntime: Attachment<HTMLDivElement> = (root) => {
    let instance: ReturnType<typeof createDialog> | undefined;
    let retainedOpen: boolean | undefined;
    $effect(() => {
      const connectionCloseOnEscape = closeOnEscape;
      const connectionCloseOnOutsideInteract = closeOnOutsideInteract;
      const connectionModal = modal;
      const acceptedOpen = externallyControlled
        ? untrack(() => renderedOpen)
        : retainedOpen ?? untrack(() => renderedOpen);
      retainedOpen = undefined;
      if (!externallyControlled) uncontrolledOpen = acceptedOpen;
      instance = createDialog(root, {
        defaultOpen: acceptedOpen,
        closeOnEscape: connectionCloseOnEscape,
        closeOnOutsideInteract: connectionCloseOnOutsideInteract,
        modal: connectionModal,
        onOpenChange: handleOpenChange,
        onCloseComplete: handleCloseComplete,
        ...(externallyControlled ? { open: acceptedOpen } : {}),
      });
      $effect(() => {
        const nextOpen = renderedOpen;
        if (!instance || Object.is(instance.getOpen(), nextOpen)) return;
        instance.setOpen(nextOpen, {"emit":false});
      });
      return () => {
        if (!instance) return;
        retainedOpen = externallyControlled ? undefined : instance.getOpen();
        instance.destroy();
        instance = undefined;
      };
    });
  };
  const attachRef: Attachment<HTMLDivElement> = (element) => {
    const callback = ref;
    untrack(() => callback?.(element));
    return () => callback?.(null);
  };
</script>

<div
  {...rest}
  data-sw-dialog=""
  data-sw-part="root"
  data-default-open={initialDefaultOpen ? "true" : undefined}
  data-close-on-escape={String(closeOnEscape)}
  data-close-on-outside-interact={String(closeOnOutsideInteract)}
  data-modal={String(modal)}
  data-state={renderedOpen ? "open" : "closed"}
  {@attach attachRuntime}
  {@attach attachRef}
>
  {@render children?.(renderedOpen)}
</div>
