<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { HTMLAttributes } from "svelte/elements";
  type Props = Omit<HTMLAttributes<HTMLDivElement>, "hidden"> & { children?: Snippet; ref?: (element: HTMLDivElement | null) => void; hiddenUntilFound?: boolean };
  let { children, ref, hiddenUntilFound = false, ...rest }: Props = $props();
  const initialHidden = untrack(() => hiddenUntilFound ? "until-found" : true);
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLDivElement>(() => ref);
</script>
<div {...rest} data-sw-collapsible-panel data-hidden-until-found={hiddenUntilFound ? "" : undefined} data-state="closed" hidden={initialHidden} {@attach attachRef}>
  {@render children?.()}
</div>
