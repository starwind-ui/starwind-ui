<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";
  import { getTimedOverlayContext } from "./PreviewCardRoot.svelte";
  type Props = Omit<HTMLAttributes<HTMLDivElement>, "children"> & { children?: Snippet; side?: "top" | "right" | "bottom" | "left"; align?: "start" | "center" | "end"; sideOffset?: number; avoidCollisions?: boolean; ref?: (element: HTMLDivElement | null) => void };
  let { children, side = "bottom", align = "center", sideOffset = 0, avoidCollisions = true, ref, ...rest }: Props = $props();
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLDivElement>(() => ref);
  const context = getTimedOverlayContext();
  const attachPlacement: Attachment<HTMLDivElement> = (element) => {
    $effect(() => {
      const attributes = { "data-side": String(side), "data-align": String(align), "data-side-offset": String(sideOffset), "data-avoid-collisions": String(avoidCollisions) };
      untrack(() => context?.registerPlacement(element, attributes));
    });
    return () => context?.registerPlacement(element, null);
  };
</script>
<div {...rest} data-sw-preview-card-popup=""
data-sw-part="popup"
data-state="closed"
data-side={side}
data-align={align}
data-side-offset={sideOffset}
data-avoid-collisions={String(avoidCollisions)}
role="tooltip"
hidden  {@attach attachPlacement} {@attach attachRef}>
  {@render children?.()}
</div>
