<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import { setContext, untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";
  import { getNavigationMenuContext, getNavigationMenuItemContext, navigationMenuItemKey, type NavigationMenuItemContext } from "./NavigationMenuRoot.svelte";
  type Props = Omit<HTMLAttributes<HTMLLIElement>, "children" | "value"> & { children?: Snippet; value?: string; ref?: (element: HTMLLIElement | null) => void };
  let { children, value, ref, ...rest }: Props = $props();
  const menu = getNavigationMenuContext(); const item = getNavigationMenuItemContext();
  const owner = Symbol("NavigationMenuItem owner");
  let currentElement = $state.raw<HTMLLIElement | null>(null);
  let open = $derived(menu?.isItemOpen(currentElement, value) ?? false);
  setContext<NavigationMenuItemContext>(navigationMenuItemKey, { get open() { return open; } });
  const attachPart: Attachment<HTMLLIElement> = (element) => {
    currentElement = element;
    $effect(() => {
      const attributes = { "data-value": value };
      untrack(() => menu?.registerPart(owner, { element, part: "item", attributes }));
      return () => menu?.registerPart(owner, null);
    });
    return () => { currentElement = null; };
  };
  let elementProps = $derived({ ...rest });
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLLIElement>(() => ref);
</script>
<li {...elementProps} data-sw-nav-menu-item="" data-sw-part="item" data-value={value} data-state={open ? "open" : "closed"} {@attach attachPart} {@attach attachRef}>{@render children?.()}</li>
