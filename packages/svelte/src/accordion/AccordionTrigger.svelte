<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLButtonAttributes } from "svelte/elements";
  import { getAccordionItemContext } from "./AccordionItemContext";

  type Props = Omit<HTMLButtonAttributes, "children" | "type"> & { children?: Snippet; ref?: (element: HTMLButtonElement | null) => void };
  let { children, disabled = false, ref, ...rest }: Props = $props();
  const item = getAccordionItemContext("AccordionTrigger");
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLButtonElement>(() => ref);
</script>

<button
  {...rest}
  data-sw-accordion-trigger=""
type="button"
aria-expanded="false"
data-state="closed"
  data-sw-part="trigger"
  disabled={item.disabled || Boolean(disabled) || rest["aria-disabled"] === true || rest["aria-disabled"] === "true"}
  {@attach attachRef}
>
  {@render children?.()}
</button>
