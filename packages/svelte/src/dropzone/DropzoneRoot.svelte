<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<script lang="ts">
  import { createDropzone, type DropzoneFilesChangeDetails } from "@starwind-ui/runtime/dropzone";
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import { observeFormDiscovery } from "../_internal/form-discovery.js";
  import type { HTMLLabelAttributes } from "svelte/elements";
  type Props = Omit<HTMLLabelAttributes, "children" | "onchange"> & {
    children?: Snippet;
    disabled?: boolean;
    isUploading?: boolean;
    onFilesChange?: (files: File[], detail: DropzoneFilesChangeDetails) => void;
    ref?: (element: HTMLLabelElement | null) => void;
  };
  let { children, disabled = false, isUploading = false, onFilesChange, ref, ...rest }: Props = $props();
  const readInputs = () => ({ disabled, isUploading });
  function connectDropzone(root: HTMLElement) {
    let previous = readInputs();
    const instance = createDropzone(root, {
      disabled: previous.disabled,
isUploading: previous.isUploading,
    });
    const unsubscribe = instance.subscribe("filesChange", (detail) => {
      untrack(() => { onFilesChange?.(detail.files, detail); });
    });
    const stopDiscovery = observeFormDiscovery(root.ownerDocument, () => {
      untrack(() => { instance.refresh(); });
    });
    return {
      update(): void {
        const next = readInputs();
        if (next.disabled !== previous.disabled) instance.setDisabled(next.disabled);
if (next.isUploading !== previous.isUploading) instance.setUploading(next.isUploading);
        previous = next;
      },
      destroy(): void {
        stopDiscovery();
        unsubscribe();
        instance.destroy();
      },
    };
  }
  const attachRuntime: Attachment<HTMLLabelElement> = (element) => {
    const connection = untrack(() => connectDropzone(element));
    $effect(() => {
      readInputs();
      untrack(() => connection.update());
    });
    return () => connection.destroy();
  };
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLLabelElement>(() => ref);
</script>
<label {...rest} data-sw-dropzone="" data-disabled={disabled ? "" : undefined}
  data-drag-active="false" data-has-files="false" data-is-uploading={String(isUploading)}
  aria-disabled={disabled} role="button" tabindex={disabled ? -1 : 0}
  {@attach attachRuntime} {@attach attachRef}>
  {@render children?.()}
</label>
