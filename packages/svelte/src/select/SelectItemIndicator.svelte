<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Svelte proof output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";
  import { useSelectItemContext, useSelectContext } from "./SelectRoot.svelte";
  type Props = Omit<HTMLAttributes<HTMLSpanElement>, "children"> & { children?: Snippet; ref?: (element: HTMLSpanElement | null) => void };
  let { children, ref, ...rest }: Props = $props();
  const select = useSelectContext("ItemIndicator");
  const item = useSelectItemContext("ItemIndicator");
  let selected = $derived(select.value === item.value);
  const initialSelected = untrack(() => selected);
  const attachRef: Attachment<HTMLSpanElement> = (element) => { const callback = ref; untrack(() => callback?.(element)); return () => callback?.(null); };
</script>
<span {...rest} data-sw-select-item-indicator="" data-sw-part="itemIndicator" aria-hidden="true" data-state={selected ? "checked" : "unchecked"} data-visible={selected ? "" : undefined} data-hidden={selected ? undefined : ""} hidden={!initialSelected} {@attach attachRef}>{@render children?.()}</span>
