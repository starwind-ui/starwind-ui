<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { HTMLTemplateAttributes } from "svelte/elements";

  type Variant = "default" | "error" | "info" | "loading" | "success" | "warning";
  type Props = Omit<HTMLTemplateAttributes, "children"> & {
    children?: Snippet;
    variant?: Variant;
    ref?: (element: HTMLTemplateElement | null) => void;
  };
  let { children, variant = "default", ref, ...rest }: Props = $props();
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLTemplateElement>(() => ref);
</script>

<template
  {...rest}
  data-sw-toast-template={variant}
  {@attach attachRef}
>
  {@render children?.()}
</template>
