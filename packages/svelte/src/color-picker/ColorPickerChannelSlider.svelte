<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<script lang="ts">
  import { projectColorPickerInitialPart, type ColorPickerInitialPartRequest, type ColorPickerChannel, type ColorPickerValue } from "@starwind-ui/runtime/color-picker";
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";
  import { getRoot, getArea, setArea, getChannel, setChannel, mergeProjection, refreshPart } from "./context.js";
  type Props = Omit<HTMLAttributes<HTMLDivElement>, "children" | "step"> & { children?: Snippet; channel?: ColorPickerChannel; orientation?: "horizontal" | "vertical"; step?: number; ref?: (element: HTMLDivElement | null) => void };
  let { children, channel = "hue", orientation = "horizontal", step, ref, ...rest }: Props = $props();
  const context = getRoot();
  setChannel({ owner: context, get channel() { return channel; }, get orientation() { return orientation; }, get step() { return step; } });
  const request = (): ColorPickerInitialPartRequest => ({ part: "channelSlider", channel: channel, orientation: orientation, step: step });
  const projection = untrack(() => projectColorPickerInitialPart(context.initialState, request()));
  let elementProps = $derived(mergeProjection(projection, rest, { "data-sw-color-picker-channel-slider": "", "data-sw-part": "channelSlider",  "data-channel": channel, "data-orientation": orientation, }));
  const attachPart: Attachment<HTMLDivElement> = (element) => {
    const release = untrack(() => context.register(element));
    $effect(() => refreshPart(context, request, rest));
    return release;
  };
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLDivElement>(() => ref);
</script>

<div {...elementProps} {@attach attachPart} {@attach attachRef}>{#if children}{@render children()}{:else}{/if}</div>
