<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<script lang="ts">import {onMount,untrack,type Snippet} from 'svelte';import type {HTMLAttributes} from 'svelte/elements';import {createTabs,type TabsValue,type TabsOrientation,type TabsValueChangeDetails} from '@starwind-ui/runtime/tabs';import {setTabsContext} from './TabsContext';
 type Props=Omit<HTMLAttributes<HTMLDivElement>,'children'> & {children?:Snippet<[TabsValue]>;value?:TabsValue;defaultValue?:TabsValue;orientation?:TabsOrientation;syncKey?:string;onValueChange?:(value:TabsValue,detail:TabsValueChangeDetails)=>void;ref?:(element:HTMLDivElement|null)=>void};let {children,value=$bindable(),defaultValue,orientation='horizontal',syncKey,onValueChange,ref,...rest}:Props=$props();const initialDefault=untrack(()=>defaultValue);
const initialSyncKey=untrack(()=>syncKey);let renderedValue=$state<TabsValue>(untrack(()=>normalizeModel(value!==undefined?value:initialDefault)));let selected=$derived(renderedValue);let rootElement=$state<HTMLDivElement>(),ready=$state(false),revision=$state(0);const connection:{instance?:ReturnType<typeof createTabs>;unsubscribe?:()=>void}={};setTabsContext({get value(){return renderedValue;},get orientation(){return orientation;},refresh(){revision+=1;}});
 function normalizeModel(next:TabsValue|undefined):TabsValue{return next??null;}
 function publish(owned:ReturnType<typeof createTabs>){if(connection.instance!==owned)return;const next=owned.getValue();renderedValue=next; if (value !== next) value = next;}
 function disconnect(){const owned=connection.instance;if(!owned)return;connection.unsubscribe?.();connection.unsubscribe=undefined;
connection.instance=undefined;
owned.destroy();}
 function connect(root:HTMLDivElement){disconnect();const desired=value!==undefined?value:initialDefault;
 const owned=createTabs(root,{defaultValue:desired,...(false?{value:desired}:{}),orientation:orientation,syncKey:initialSyncKey,onValueChange:(next,detail)=>{untrack(() => { onValueChange?.(next, detail); });}});connection.instance=owned;
 connection.unsubscribe=owned.subscribe('valueChange',detail=>{if(connection.instance!==owned||detail.isCanceled)return;untrack(() => { renderedValue=detail.value; if (value !== detail.value) value = detail.value; });});if(value!==undefined&&owned.getValue()!==desired)owned.setValue(desired??null,{emit:false,sync:false});publish(owned);}
 function applyParent(){const owned=connection.instance,next=value;if(!owned||next===undefined||owned.getValue()===next)return;owned.refresh();if(owned.getValue()!==next)owned.setValue(next,{emit:false,sync:true});publish(owned);}
 function refresh(){const owned=connection.instance;if(!owned)return;owned.refresh();publish(owned);}
 function serialize(next:TabsValue|undefined){return next===null?'null':next;}
 onMount(()=>{if(rootElement)untrack(()=>connect(rootElement!));ready=true;return()=>{ready=false;disconnect();};});$effect(()=>{ready;value;untrack(applyParent);});$effect(()=>{ready;orientation;revision;untrack(refresh);});$effect(()=>{const element=rootElement,callback=ref;if(!element)return;untrack(()=>callback?.(element));return()=>untrack(()=>callback?.(null));});
 </script><div bind:this={rootElement} {...rest} data-sw-tabs={""} data-sw-part={"root"} data-default-value={serialize(initialDefault)} data-sync-key={initialSyncKey} data-value={serialize(selected)} data-orientation={orientation}>{@render children?.(renderedValue)}</div>
