<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { HTMLAttributes } from "svelte/elements";
  type Props = HTMLAttributes<HTMLDivElement> & { children?: Snippet; ref?: (element: HTMLDivElement | null) => void; keepMounted?: boolean; orientation?: "horizontal" | "vertical"; };
  let { children, ref, keepMounted = false, orientation = "vertical", ...rest }: Props = $props();

  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLDivElement>(() => ref);
</script>
<div {...rest} data-sw-scroll-area-scrollbar aria-hidden="true" data-keep-mounted={keepMounted ? "" : undefined} data-orientation={orientation} {@attach attachRef}>
  {@render children?.()}
</div>
