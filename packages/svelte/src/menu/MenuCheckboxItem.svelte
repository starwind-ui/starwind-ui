<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import type { MenuCheckedChangeDetails } from "@starwind-ui/runtime/menu";
  import { setContext, untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";
  import { getMenuTreeContext, menuCheckedKey } from "./MenuRoot.svelte";
  type Props = Omit<HTMLAttributes<HTMLDivElement>, "children"> & {
    children?: Snippet; checked?: boolean; defaultChecked?: boolean; disabled?: boolean; closeOnClick?: boolean;
    onCheckedChange?: (checked: boolean, detail: MenuCheckedChangeDetails) => void;
    ref?: (element: HTMLDivElement | null) => void;
  };
  let { children, checked = $bindable(), defaultChecked, disabled = false, closeOnClick = false, onCheckedChange, ref, ...rest }: Props = $props();
  const initial = untrack(() => checked);
  let accepted = $state(initial ?? untrack(() => defaultChecked) ?? false);
  const tree = getMenuTreeContext();
  const owner = Symbol("MenuCheckboxItem model");
  setContext(menuCheckedKey, { get checked() { return accepted; } });
  const attachModel: Attachment<HTMLDivElement> = (element) => {
    let alive = true;
    function project(next: boolean | undefined): void {
      if (next === undefined) return;
      element.setAttribute("aria-checked",String(next));
   element.toggleAttribute("data-checked",next);
   element.toggleAttribute("data-unchecked",!next);
   for (const indicator of element.querySelectorAll<HTMLElement>("[data-sw-menu-checkbox-item-indicator]")) {
     if (indicator.closest("[data-sw-menu-checkbox-item]") !== element) continue;
     indicator.setAttribute("aria-hidden","true");
     indicator.setAttribute("data-state",next?"checked":"unchecked");
     indicator.toggleAttribute("data-visible",next);
     indicator.toggleAttribute("data-hidden",!next);
   }
    }
    function prepare(): void {
      if (checked !== undefined) accepted = checked;
      project(accepted);
    }
    function accept(): void { if (alive) accepted = element.getAttribute("aria-checked") === "true"; }
    function handle(event: Event): void { if (event.target !== element) return;
   const ownerElement = element; if (!ownerElement) return;
   const details = (event as CustomEvent<MenuCheckedChangeDetails>).detail;

   untrack(() => { onCheckedChange?.(details.checked,details); });
   queueMicrotask(() => {

     if (!(alive && ownerElement.isConnected) || details.isCanceled) return;
     untrack(() => { accepted=details.checked;
       if (checked !== details.checked) checked=details.checked; });


   }); }
    untrack(prepare);
    tree?.registerModel(owner, {prepare,accept});
    element.addEventListener("starwind:checked-change",handle);
    $effect(() => { void checked; untrack(prepare); });
    return () => { alive = false; tree?.registerModel(owner, null); element.removeEventListener("starwind:checked-change", handle); };
  };
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLDivElement>(() => ref);
</script>
<div {...rest} data-sw-menu-checkbox-item="" data-sw-part="checkboxItem" role="menuitemcheckbox" tabindex="0" aria-checked={accepted} data-checked={accepted ? "" : undefined} data-unchecked={accepted ? undefined : ""} data-default-checked={accepted ? "true" : undefined} data-close-on-click={String(closeOnClick)} aria-disabled={disabled ? "true" : undefined} data-disabled={disabled ? "" : undefined} {@attach attachModel} {@attach attachRef}>{@render children?.()}</div>
