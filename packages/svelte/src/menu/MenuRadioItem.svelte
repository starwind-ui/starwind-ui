<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import { setContext, untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";
  import { getMenuRadioContext, menuCheckedKey } from "./MenuRoot.svelte";
  type Props = Omit<HTMLAttributes<HTMLDivElement>, "children"> & { children?: Snippet; value: string; checked?: boolean; defaultChecked?: boolean; disabled?: boolean; closeOnClick?: boolean; ref?: (element: HTMLDivElement | null) => void };
  let { children, value, checked, defaultChecked = false, disabled = false, closeOnClick = false, ref, ...rest }: Props = $props();
  const group = getMenuRadioContext();
  const initialDefault = untrack(() => defaultChecked);
  let accepted = $derived(group?.value === undefined ? (checked ?? initialDefault) : group?.value === value);
  setContext(menuCheckedKey, { get checked() { return accepted; } });
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLDivElement>(() => ref);
</script>
<div {...rest} data-sw-menu-radio-item="" data-sw-part="radioItem" role="menuitemradio" tabindex="0" data-value={value} data-default-checked={initialDefault ? "true" : undefined} data-close-on-click={String(closeOnClick)} aria-checked={accepted} data-checked={accepted ? "" : undefined} data-unchecked={accepted ? undefined : ""} aria-disabled={disabled ? "true" : undefined} data-disabled={disabled ? "" : undefined} {@attach attachRef}>{@render children?.()}</div>
