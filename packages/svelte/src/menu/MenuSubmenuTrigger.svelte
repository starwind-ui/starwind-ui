<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";
  import { getMenuTreeContext, getMenuOwnerContext } from "./MenuRoot.svelte";
  type Props = Omit<HTMLAttributes<HTMLDivElement>, "children"> & { children?: Snippet; ref?: (element: HTMLDivElement | null) => void;  disabled?: boolean;  };
  let { children, ref,   disabled = false,  ...rest }: Props = $props();
  const tree = getMenuTreeContext(); const menu = getMenuOwnerContext(); const owner = Symbol("submenuTrigger");
  const attachPart: Attachment<HTMLDivElement> = (element) => {
    $effect(() => {
      const attributes = undefined;
      untrack(() => tree?.registerPart(owner, { element, scope: menu!.scope, part: "submenuTrigger", attributes }));
      return () => tree?.registerPart(owner, null);
    });
  };
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLDivElement>(() => ref);
</script>
<div {...rest} data-sw-menu-submenu-trigger="" data-sw-part="submenuTrigger" tabindex="0" aria-disabled={disabled ? "true" : undefined} data-disabled={disabled ? "" : undefined} role="menuitem" aria-haspopup="menu" aria-expanded="false" data-state="closed" {@attach attachPart} {@attach attachRef}>{@render children?.()}</div>
