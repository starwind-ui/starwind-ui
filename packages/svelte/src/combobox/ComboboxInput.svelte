<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Svelte 5 public beta adapter output. -->
<script lang="ts">
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLInputAttributes } from "svelte/elements";
  import { getComboboxContext } from "./ComboboxRoot.svelte";
  type Props = Omit<HTMLInputAttributes, "children" | "defaultValue"> & {  defaultValue?: string; ref?: (element: HTMLInputElement | null) => void };
  let {  defaultValue, disabled = false, readonly: nativeReadOnly = false, ref, ...rest }: Props = $props();
  const combobox = getComboboxContext();

  const owner = Symbol("ComboboxInput owner");
  const attachPart: Attachment<HTMLInputElement> = (element) => {
    $effect(() => {
      const attributes = {};
      untrack(() => combobox?.registerPart(owner, { element, part: "input", attributes }));
      return () => combobox?.registerPart(owner, null);
    });
  };
  let elementProps = $derived({ ...rest });
  import { createRefAttachment as createAttachRef } from "../_internal/ref-attachment.js";
  const attachRef = createAttachRef<HTMLInputElement>(() => ref);
</script>
<input {...elementProps} data-sw-combobox-input="" data-sw-part="input" role="combobox" autocomplete="off" aria-autocomplete="list" aria-expanded={combobox?.open ?? false} disabled={disabled || combobox?.disabled} readonly={nativeReadOnly || combobox?.readOnly} value={combobox?.inputValue ?? defaultValue ?? ""} {@attach attachPart} {@attach attachRef} />
