<!-- Generated by scripts/portable-runtime/generate-svelte-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Svelte proof output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script lang="ts">
  import { type AccordionValue, type AccordionValueChangeDetails, createAccordion } from "@starwind-ui/runtime/accordion";
  import { untrack, type Snippet } from "svelte";
  import type { Attachment } from "svelte/attachments";
  import type { HTMLAttributes } from "svelte/elements";

  type Props = Omit<HTMLAttributes<HTMLDivElement>, "children"> & {
    children?: Snippet<[AccordionValue]>;
    type?: "single" | "multiple";
    defaultValue?: AccordionValue;
    value?: AccordionValue;
    collapsible?: boolean;
    onValueChange?: (value: AccordionValue, detail: AccordionValueChangeDetails) => void;
    ref?: (element: HTMLDivElement | null) => void;
  };

  let { children, type = "single", defaultValue, value = $bindable(), collapsible = true, onValueChange, ref, ...rest }: Props = $props();
  const externallyControlled = untrack(() => value !== undefined);
  const initialDefaultValue = untrack(() => defaultValue);
  let uncontrolledValue = $state<AccordionValue>(initialDefaultValue ?? null);
  let renderedValue = $derived(value !== undefined ? value : uncontrolledValue);
  const defaultValueAttribute = Array.isArray(initialDefaultValue) ? JSON.stringify(initialDefaultValue) : initialDefaultValue;

  function handleValueChange(detail: AccordionValueChangeDetails): void {
    const nextValue = detail.value;
    onValueChange?.(nextValue, detail);
    if (detail.isCanceled) return;
    if (!externallyControlled) uncontrolledValue = nextValue;
    value = nextValue;
  }

  const attachRuntime: Attachment<HTMLDivElement> = (root) => {
    $effect(() => {
      const connectionType = type;
      const connectionCollapsible = collapsible;
      const initialValue = untrack(() => renderedValue);
      const instance = createAccordion(root, {
        type: connectionType,
        defaultValue: initialDefaultValue,
        collapsible: connectionCollapsible,
        ...(externallyControlled ? { value: initialValue } : {}),
        onValueChange: handleValueChange,
      });
      $effect(() => {
        const nextValue = renderedValue;
        if (isAccordionValueEqual(instance.getValue(), nextValue)) return;
        instance.setValue(nextValue, {"emit":false});
      });
      return () => instance.destroy();
    });
  };
  const attachRef: Attachment<HTMLDivElement> = (element) => {
    const callback = ref;
    untrack(() => callback?.(element));
    return () => callback?.(null);
  };

  function isAccordionValueEqual(left: AccordionValue, right: AccordionValue): boolean {
    if (Array.isArray(left) || Array.isArray(right)) return JSON.stringify(left) === JSON.stringify(right);
    return left === right;
  }
</script>

<div
  {...rest}
  data-sw-accordion=""
  data-sw-part="root"
  data-type={type}
  data-default-value={defaultValueAttribute}
  data-collapsible={String(collapsible)}
  data-state="closed"
  {@attach attachRuntime}
  {@attach attachRef}
>
  {@render children?.(renderedValue)}
</div>
