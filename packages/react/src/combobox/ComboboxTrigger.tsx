/**
 * Generated by scripts/portable-runtime/generate-react-wrappers.ts.
 * Do not edit by hand; update the contract/template instead.
 */

import * as React from "react";
import type { RefCapableElementProps } from "../internal/compose-refs";
import {
  getAsChildElement,
  getElementRef,
  mergeAsChildProps,
  useComposedRefs,
} from "../internal/compose-refs";

import { useComboboxContext } from "./ComboboxContext";

export type ComboboxTriggerProps = React.ButtonHTMLAttributes<HTMLButtonElement> & {
  asChild?: boolean;
};

const ComboboxTrigger = React.forwardRef<HTMLElement, ComboboxTriggerProps>(
  function ComboboxTrigger({ asChild = false, children, className, ...props }, forwardedRef) {
    const combobox = useComboboxContext();
    const protectedTriggerProps = {
      "data-sw-combobox-trigger": "",
      "aria-disabled": combobox.disabled ? "true" : undefined,
      "aria-readonly": combobox.readOnly ? "true" : undefined,
      "data-disabled": combobox.disabled ? "" : undefined,
      "data-readonly": combobox.readOnly ? "" : undefined,
      "aria-haspopup": "listbox",
      "aria-expanded": combobox.open ? "true" : "false",
      "data-state": combobox.open ? "open" : "closed",
      disabled: combobox.disabled || undefined,
    } satisfies React.HTMLAttributes<HTMLElement> & { disabled?: boolean } & Record<
        `data-${string}`,
        string | undefined
      >;
    const triggerProps = {
      ...protectedTriggerProps,
      ...props,
    } satisfies React.HTMLAttributes<HTMLElement> & { disabled?: boolean } & Record<
        `data-${string}`,
        string | undefined
      >;

    const asChildElement = getAsChildElement(children);
    const composedRef = useComposedRefs(
      forwardedRef,
      asChildElement ? getElementRef(asChildElement) : undefined,
    );

    if (asChild && asChildElement) {
      const child = asChildElement;
      const childProps = child.props;
      const mergedProps = mergeAsChildProps({ ...triggerProps, className }, childProps, {
        protectedProps: protectedTriggerProps,
      });

      return React.cloneElement(child, {
        ...mergedProps,
        ref: composedRef,
      });
    }

    return (
      <button
        type="button"
        className={className}
        ref={forwardedRef as React.Ref<HTMLButtonElement>}
        {...(triggerProps as React.ButtonHTMLAttributes<HTMLButtonElement>)}
        disabled={combobox.disabled}
      >
        {children}
      </button>
    );
  },
);

ComboboxTrigger.displayName = "Combobox.Trigger";

export default ComboboxTrigger;
