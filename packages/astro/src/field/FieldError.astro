---
/**
 * Generated by scripts/portable-runtime/generate-astro-wrappers.ts.
 * Do not edit by hand; update the contract/template instead.
 */
import type { HTMLAttributes } from "astro/types";

type FieldErrorMatch =
  | boolean
  | "badInput"
  | "customError"
  | "patternMismatch"
  | "rangeOverflow"
  | "rangeUnderflow"
  | "stepMismatch"
  | "tooLong"
  | "tooShort"
  | "typeMismatch"
  | "valid"
  | "valueMissing";

type FieldErrorMessageSource = "children" | "validation";

interface Props extends HTMLAttributes<"div"> {
  match?: FieldErrorMatch;
  messageSource?: FieldErrorMessageSource;
}

const { hidden = true, match = false, messageSource, ...rest } = Astro.props;
const serializedMatch = typeof match === "boolean" ? String(match) : match;
---

<div
  data-sw-field-error
  data-match={serializedMatch}
  data-message-source={messageSource}
  hidden={hidden}
  {...rest}
>
  <slot />
</div>
