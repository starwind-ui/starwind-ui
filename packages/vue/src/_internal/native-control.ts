/**
 * Generated by scripts/portable-runtime/generate-vue-wrappers.ts.
 * Do not edit by hand; update the contract/template instead.
 */

import {
  type ComponentPublicInstance,
  cloneVNode,
  isRef,
  isVNode,
  mergeProps,
  onBeforeUnmount,
  ref,
  type VNode,
  watch,
} from "vue";

type ComponentRoot = ComponentPublicInstance & {
  element?: unknown;
};

type VueNativeControlRenderOptions = {
  children: readonly VNode[];
  consumerProps?: Record<string, unknown>;
  defaultedProps?: Record<string, unknown>;
  defaultNativeButtonType?: string;
  protectedProps?: Record<string, unknown>;
};

export function useVueNativeControl(
  name: string,
  requestRefresh?: () => void,
  allowComponentRoot = false,
) {
  const element = ref<HTMLElement | null>(null);

  watch(element, () => requestRefresh?.(), { flush: "post" });
  onBeforeUnmount(() => requestRefresh?.());

  function render({
    children,
    consumerProps = {},
    defaultedProps = {},
    defaultNativeButtonType,
    protectedProps = {},
  }: VueNativeControlRenderOptions): VNode {
    const child = children[0];
    if (
      children.length !== 1 ||
      !isVNode(child) ||
      (typeof child.type !== "string" &&
        !(
          allowComponentRoot &&
          (typeof child.type === "object" || typeof child.type === "function")
        ))
    ) {
      throw new TypeError(
        `${name} asChild requires exactly one ${allowComponentRoot ? "native element or component" : "native element"} VNode.`,
      );
    }
    const nativeButtonDefault =
      defaultNativeButtonType !== undefined &&
      child.type === "button" &&
      child.props?.type === undefined &&
      consumerProps.type === undefined
        ? { type: defaultNativeButtonType }
        : {};
    return cloneVNode(
      child,
      mergeProps(defaultedProps, nativeButtonDefault, consumerProps, protectedProps, {
        ref: setElement,
      }),
      true,
    );
  }

  function setElement(value: Element | ComponentPublicInstance | null): void {
    if (value === null) {
      element.value = null;
      return;
    }
    const nativeElement = resolveNativeElement(value);
    if (nativeElement) {
      element.value = nativeElement;
      return;
    }
    element.value = null;
    throw new TypeError(
      `${name} asChild component must expose an HTMLElement as "element" or render one native HTMLElement root. Rootless and multiple-root components are invalid.`,
    );
  }

  return { element, render, setElement } as const;
}

function resolveNativeElement(value: Element | ComponentPublicInstance): HTMLElement | null {
  if (value instanceof HTMLElement) return value;

  const component = value as ComponentRoot;
  const exposedElement = isRef(component.element) ? component.element.value : component.element;
  if (exposedElement instanceof HTMLElement) return exposedElement;

  const publicRoot = component.$el;
  return publicRoot instanceof HTMLElement ? publicRoot : null;
}
