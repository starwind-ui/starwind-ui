<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Vue adapter output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script setup lang="ts">
import { defineComponent, ref, useAttrs, type VNode } from "vue";
import { createVueAsChild } from "../_internal/as-child";
import { useNavigationMenuItemContext } from "./NavigationMenuRoot.vue";

defineOptions({ inheritAttrs: false });
const props = withDefaults(
  defineProps<{ asChild?: boolean; disabled?: boolean; openDelay?: number; closeDelay?: number }>(),
  { asChild: false, disabled: false },
);
const slots = defineSlots<{ default?: () => VNode[] }>();
const attrs = useAttrs();
const item = useNavigationMenuItemContext("NavigationMenuTrigger");
const element = ref<HTMLElement | null>(null);
const asChild = createVueAsChild("NavigationMenuTrigger", element);
const { setElement } = asChild;
const AsChild = defineComponent({
  setup() {
    return () =>
      asChild.render({
        children: slots.default?.() ?? [],
        consumerProps: attrs,
        protectedProps: protectedAttrs(),
      });
  },
});
function protectedAttrs() {
  return {
    "data-sw-nav-menu-trigger": "",
    "data-open-delay": props.openDelay == null ? undefined : String(props.openDelay),
    "data-close-delay": props.closeDelay == null ? undefined : String(props.closeDelay),
    "data-disabled": props.disabled ? "" : undefined,
    "aria-disabled": props.disabled ? true : undefined,
    "aria-expanded": item.open.value,
    "aria-haspopup": "menu" as const,
    "data-state": item.open.value ? "open" : "closed",
  };
}
defineExpose({ element });
</script>
<template>
  <AsChild v-if="props.asChild" /><button
    v-else
    :ref="setElement"
    v-bind="{ ...attrs, ...protectedAttrs() }"
    type="button"
    :disabled="props.disabled"
  >
    <slot />
  </button>
</template>
