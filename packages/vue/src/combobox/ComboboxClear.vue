<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script setup lang="ts">
import { defineComponent, ref, useAttrs, type VNode } from "vue";
import { createVueAsChild } from "../_internal/as-child";
import { useComboboxContext } from "./ComboboxRoot.vue";

defineOptions({ inheritAttrs: false });
const props = withDefaults(defineProps<{ asChild?: boolean }>(), { asChild: false });
const slots = defineSlots<{ default?: () => VNode[] }>();
const attrs = useAttrs();
const element = ref<HTMLElement | null>(null);
const combobox = useComboboxContext("Clear");
const asChild = createVueAsChild("ComboboxClear", element);
const { setElement } = asChild;
defineExpose({ element });
function protectedAttrs(): Record<string, unknown> {
  return {
    "data-sw-combobox-clear": "",
    "data-sw-part": "clear",
    "aria-disabled": combobox.disabled.value ? "true" : undefined,
    "aria-readonly": combobox.readOnly.value ? "true" : undefined,
    "data-disabled": combobox.disabled.value ? "" : undefined,
    "data-readonly": combobox.readOnly.value ? "" : undefined,
    disabled: combobox.disabled.value || undefined,
  };
}
const AsChild = defineComponent({
  inheritAttrs: false,
  setup() {
    return () =>
      asChild.render({
        children: slots.default?.() ?? [],
        consumerProps: attrs,
        defaultNativeButtonType: "button",
        protectedProps: protectedAttrs(),
      });
  },
});
</script>
<template>
  <AsChild v-if="props.asChild" />
  <button v-else :ref="setElement" v-bind="{ ...attrs, ...protectedAttrs() }" type="button">
    <slot />
  </button>
</template>
