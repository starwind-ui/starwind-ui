<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script setup lang="ts">
import { ref } from "vue";
import { useComboboxContext } from "./ComboboxRoot.vue";

defineOptions({ inheritAttrs: false });
const props = defineProps<{ placeholder?: string }>();
const slots = defineSlots<{ default?: () => unknown }>();
const element = ref<HTMLSpanElement | null>(null);
const initialPlaceholder = props.placeholder;
useComboboxContext("Value");
defineExpose({ element });
</script>
<template>
  <span
    ref="element"
    v-bind="$attrs"
    :data-sw-combobox-value="slots.default ? undefined : ''"
    data-sw-part="value"
    :data-placeholder="props.placeholder"
    ><slot>{{ initialPlaceholder }}</slot></span
  >
</template>
