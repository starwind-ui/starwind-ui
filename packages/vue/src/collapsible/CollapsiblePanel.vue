<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Vue adapter output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script setup lang="ts">
import { ref } from "vue";

defineOptions({ inheritAttrs: false });

const props = withDefaults(defineProps<{ hiddenUntilFound?: boolean }>(), {
  hiddenUntilFound: false,
});
defineSlots<{ default?: () => unknown }>();
const element = ref<HTMLDivElement | null>(null);

defineExpose({ element });
</script>

<template>
  <div
    ref="element"
    v-bind="$attrs"
    data-sw-collapsible-panel
    data-sw-part="panel"
    :data-hidden-until-found="props.hiddenUntilFound ? '' : undefined"
    data-state="closed"
    :hidden="props.hiddenUntilFound ? 'until-found' : true"
  >
    <slot />
  </div>
</template>
