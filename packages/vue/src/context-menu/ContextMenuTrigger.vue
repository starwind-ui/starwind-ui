<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script setup lang="ts">
import { ref, useAttrs } from "vue";
import { useMenuRootContext } from "../menu/MenuContext";

defineOptions({ inheritAttrs: false });
const props = withDefaults(defineProps<{ disabled?: boolean; tabIndex?: number }>(), {
  disabled: false,
  tabIndex: 0,
});
defineSlots<{ default?: () => unknown }>();
const publicAttrs = useAttrs();
const menu = useMenuRootContext("ContextMenuTrigger");
const element = ref<HTMLDivElement | null>(null);
defineExpose({ element });
</script>
<template>
  <div
    ref="element"
    v-bind="publicAttrs"
    data-sw-context-menu-trigger
    data-sw-menu-trigger
    data-sw-part="trigger"
    aria-haspopup="menu"
    :aria-expanded="menu.open.value"
    :aria-disabled="props.disabled ? 'true' : undefined"
    :data-disabled="props.disabled ? '' : undefined"
    :data-state="menu.open.value ? 'open' : 'closed'"
    :tabindex="props.disabled ? -1 : props.tabIndex"
    :style="[{ WebkitTouchCallout: 'none' }, publicAttrs.style]"
  >
    <slot />
  </div>
</template>
