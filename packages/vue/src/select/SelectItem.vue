<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script setup lang="ts">
import { computed, provide, ref } from "vue";
import { SelectItemContext, useSelectContext } from "./SelectRoot.vue";

defineOptions({ inheritAttrs: false });
const props = withDefaults(defineProps<{ disabled?: boolean; value: string }>(), {
  disabled: false,
});
defineSlots<{ default?: () => unknown }>();
const itemRef = ref<HTMLDivElement | null>(null);
const select = useSelectContext("Item");
const value = computed(() => props.value);
const disabled = computed(() => props.disabled);
const selected = computed(() => select.value.value === value.value);
provide(SelectItemContext, { disabled, value });
defineExpose({ element: itemRef });
</script>

<template>
  <div
    ref="itemRef"
    v-bind="$attrs"
    data-sw-select-item
    data-sw-part="item"
    :data-value="props.value"
    role="option"
    :aria-selected="selected"
    :aria-disabled="props.disabled ? 'true' : undefined"
    :data-disabled="props.disabled ? '' : undefined"
    :data-selected="selected ? '' : undefined"
    tabindex="-1"
  >
    <slot />
  </div>
</template>
