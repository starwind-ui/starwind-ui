<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script lang="ts">
import { inject } from "vue";
import { useVueNativeControl } from "../_internal/native-control";
import { AlertDialogContext } from "./AlertDialogRoot.vue";

export function __useAlertDialogControl(name: string) {
  const root = inject(AlertDialogContext, undefined);
  return useVueNativeControl(name, () => root?.requestRefresh());
}
</script>
<script setup lang="ts">
import { defineComponent, useAttrs, type HTMLAttributes, type VNode } from "vue";

defineOptions({ inheritAttrs: false });

type NativeElementProps = /* @vue-ignore */ HTMLAttributes;
const props = withDefaults(defineProps<{ asChild?: boolean } & NativeElementProps>(), {
  asChild: false,
});
const slots = defineSlots<{ default?: () => VNode[] }>();
const attrs = useAttrs();
const { element, render: renderAsChild, setElement } = __useAlertDialogControl("AlertDialogClose");
const AsChildControl = defineComponent({
  inheritAttrs: false,
  setup() {
    return () =>
      renderAsChild({
        children: slots.default?.() ?? [],
        consumerProps: attrs,
        defaultNativeButtonType: "button",
        protectedProps: {
          "data-sw-alert-dialog-close": "",
          "data-sw-part": "close",
        },
      });
  },
});

defineExpose({ element });
</script>

<template>
  <AsChildControl v-if="props.asChild" />
  <button
    :ref="setElement"
    v-else
    v-bind="$attrs"
    data-sw-alert-dialog-close
    data-sw-part="close"
    type="button"
  >
    <slot />
  </button>
</template>
