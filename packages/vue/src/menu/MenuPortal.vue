<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Vue adapter output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script setup lang="ts">
import { reportPortalPlacement, resolvePortalPlacement } from "@starwind-ui/runtime/menu";
import { onBeforeUnmount, onMounted, ref } from "vue";
import { useVuePortalPlacement } from "../_internal/portal";
import { useMenuOwnerContext, useMenuRootContext } from "./MenuContext";

defineOptions({ inheritAttrs: false });
const props = withDefaults(
  defineProps<{ container?: string | HTMLElement; disabled?: boolean }>(),
  { disabled: false },
);
defineSlots<{ default?: () => unknown }>();
const menu = useMenuRootContext("Portal");
const ownerContext = useMenuOwnerContext("Portal");
const portalRef = ref<HTMLDivElement | null>(null);
const owner = Symbol("MenuPortalOwner");
const placement = useVuePortalPlacement({
  active: () => menu.mounted.value,
  container: () => props.container,
  disabled: () => props.disabled,
  element: portalRef,
  reference: () => menu.element.value,
  runtime: { reportPortalPlacement, resolvePortalPlacement },
});
onMounted(() => {
  if (ownerContext.kind === "root") menu.registerPortal(owner, portalRef.value);
});
onBeforeUnmount(() => {
  if (ownerContext.kind === "root") menu.registerPortal(owner, null);
});
defineExpose({ element: portalRef });
</script>
<template>
  <Teleport :to="placement.target.value" :disabled="placement.disabled.value"
    ><div
      ref="portalRef"
      v-bind="$attrs"
      data-sw-menu-portal
      :data-container="typeof props.container === 'string' ? props.container : undefined"
      :data-disabled="props.disabled ? '' : undefined"
      :data-placement="placement.ready.value ? 'ready' : 'pending'"
      data-sw-portal-placement="framework"
      data-sw-part="portal"
      data-floating-root
    >
      <slot /></div
  ></Teleport>
</template>
