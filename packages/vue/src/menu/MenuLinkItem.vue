<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Vue adapter output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script setup lang="ts">
import { ref, useAttrs } from "vue";

defineOptions({ inheritAttrs: false });
const props = withDefaults(
  defineProps<{ href?: string; closeOnClick?: boolean; disabled?: boolean }>(),
  { closeOnClick: false, disabled: false },
);
defineSlots<{ default?: () => unknown }>();
const attrs = useAttrs();
const element = ref<HTMLAnchorElement | null>(null);
defineExpose({ element });
</script>
<template>
  <a
    ref="element"
    v-bind="attrs"
    data-sw-menu-link-item
    data-sw-part="linkItem"
    role="menuitem"
    tabindex="0"
    :href="props.disabled ? undefined : props.href"
    :data-close-on-click="props.closeOnClick ? 'true' : undefined"
    :aria-disabled="props.disabled ? 'true' : undefined"
    :data-disabled="props.disabled ? '' : undefined"
    ><slot
  /></a>
</template>
