<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script setup lang="ts">
import { ref } from "vue";
import { useMenuRadioItemContext } from "./MenuContext";

defineOptions({ inheritAttrs: false });
defineSlots<{ default?: () => unknown }>();
const item = useMenuRadioItemContext("RadioItemIndicator");
const element = ref<HTMLElement | null>(null);
defineExpose({ element });
</script>
<template>
  <span
    ref="element"
    v-bind="$attrs"
    data-sw-menu-radio-item-indicator
    data-sw-part="radioItemIndicator"
    aria-hidden="true"
    :data-state="item.checked.value ? 'checked' : 'unchecked'"
    :data-visible="item.checked.value ? '' : undefined"
    :data-hidden="item.checked.value ? undefined : ''"
    ><slot
  /></span>
</template>
