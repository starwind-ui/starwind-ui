<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script setup lang="ts">
import { ref } from "vue";

defineOptions({ inheritAttrs: false });
const props = withDefaults(defineProps<{ closeOnClick?: boolean; disabled?: boolean }>(), {
  closeOnClick: true,
  disabled: false,
});
defineSlots<{ default?: () => unknown }>();
const element = ref<HTMLDivElement | null>(null);
defineExpose({ element });
</script>
<template>
  <div
    ref="element"
    v-bind="$attrs"
    data-sw-menu-item
    data-sw-part="item"
    role="menuitem"
    tabindex="0"
    :data-close-on-click="props.closeOnClick ? undefined : 'false'"
    :aria-disabled="props.disabled ? 'true' : undefined"
    :data-disabled="props.disabled ? '' : undefined"
  >
    <slot />
  </div>
</template>
