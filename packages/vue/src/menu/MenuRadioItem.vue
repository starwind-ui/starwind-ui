<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<script setup lang="ts">
import { computed, provide, ref } from "vue";
import { MenuRadioItemContext, useMenuRadioGroupContext } from "./MenuContext";

defineOptions({ inheritAttrs: false });
const props = withDefaults(
  defineProps<{
    value: string;
    checked?: boolean;
    defaultChecked?: boolean;
    closeOnClick?: boolean;
    disabled?: boolean;
  }>(),
  { checked: undefined, defaultChecked: false, closeOnClick: false, disabled: false },
);
defineSlots<{ default?: () => unknown }>();
const element = ref<HTMLDivElement | null>(null);
const group = useMenuRadioGroupContext("RadioItem");
const checked = computed(() =>
  group.value.value === undefined
    ? (props.checked ?? props.defaultChecked)
    : group.value.value === props.value,
);
provide(MenuRadioItemContext, { checked });
defineExpose({ element });
</script>
<template>
  <div
    ref="element"
    v-bind="$attrs"
    data-sw-menu-radio-item
    data-sw-part="radioItem"
    role="menuitemradio"
    tabindex="0"
    :data-value="props.value"
    :data-default-checked="props.defaultChecked ? 'true' : undefined"
    :data-close-on-click="props.closeOnClick ? 'true' : undefined"
    :aria-checked="checked"
    :data-checked="checked ? '' : undefined"
    :data-unchecked="checked ? undefined : ''"
    :aria-disabled="props.disabled ? 'true' : undefined"
    :data-disabled="props.disabled ? '' : undefined"
  >
    <slot />
  </div>
</template>
