<!-- Generated by scripts/portable-runtime/generate-vue-wrappers.ts. Do not edit by hand. -->
<!-- Internal non-shipping Vue adapter output. Do not publish, expose through the CLI registry, claim in public docs, or copy into public demo dependencies. -->
<script setup lang="ts">
import { inject, onBeforeUnmount, onMounted, ref, useAttrs } from "vue";
import { PopoverContext } from "./PopoverRoot.vue";

defineOptions({ inheritAttrs: false });

const props = withDefaults(
  defineProps<{ container?: string | HTMLElement; disabled?: boolean }>(),
  { container: "body", disabled: false },
);
defineSlots<{ default?: () => unknown }>();
const attrs = useAttrs();
const root = inject(PopoverContext);
if (!root) throw new TypeError("PopoverPortal must be nested inside PopoverRoot.");
const owner = Symbol("PopoverPortal");
const element = ref<HTMLDivElement | null>(null);

onMounted(() => root.registerPortal(owner, element.value));
onBeforeUnmount(() => root.registerPortal(owner, null));

defineExpose({ element });
</script>

<template>
  <Teleport :to="props.container" :disabled="props.disabled || !root.mounted.value">
    <div ref="element" v-bind="attrs" data-sw-popover-portal data-sw-part="portal">
      <slot />
    </div>
  </Teleport>
</template>
