---
import type { HTMLAttributes } from "astro/types";
import { skeletonVariant } from "./index";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div class={skeletonVariant({ class: className })} {...rest}></div>

