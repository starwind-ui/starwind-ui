---
import type { HTMLAttributes } from "astro/types";
import { cardTitleVariant } from "./index";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div class={cardTitleVariant({ class: className })} {...rest}>
  <slot />
</div>
