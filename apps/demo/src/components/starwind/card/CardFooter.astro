---
import type { HTMLAttributes } from "astro/types";
import { cardFooterVariant } from "./index";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div class={cardFooterVariant({ class: className })} {...rest}>

  <slot />
</div>
