---
import type { HTMLAttributes } from "astro/types";
import { cardContentVariant } from "./index";


type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div class={cardContentVariant({ class: className })} {...rest}>
  <slot />
</div>
