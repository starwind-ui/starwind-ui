---
import type { HTMLAttributes } from "astro/types";
import { cardVariant } from "./index";


type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div class={cardVariant({ class: className })} {...rest}>
  <slot />
</div>
