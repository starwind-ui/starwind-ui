<script module lang="ts">
  import type { ComponentProps, Snippet } from "svelte";
  import type { VariantProps } from "tailwind-variants";
  import { cx } from "tailwind-variants";
  import { SelectGroup } from "@starwind-ui/svelte/select";
  import { select, selectContent, selectGroup, selectItem, selectItemIndicator, selectItemText, selectLabel, selectList, selectScrollButton, selectSeparator, selectTrigger, selectValue } from "./variants.js";

  export type SelectGroupProps = Omit<ComponentProps<typeof SelectGroup>, "children"> & { children?: Snippet;   };
</script>

<script lang="ts">
  let {
    "class": className,
    "children": children,
    ...rest
  }: SelectGroupProps = $props();
</script>

<SelectGroup
  class={selectGroup({ "class": cx(className) })}
  {...rest}
  data-slot={"select-group"}
>
  {@render children?.()}
</SelectGroup>
