---
trigger: glob
globs:
  - "packages/core/src/components/**"
  - "apps/demo/src/components/starwind/**"
  - "packages/core/src/registry.json"
---

# Starwind Components

Use the project-local skill at `.agents/skills/starwind-components/SKILL.md` for Starwind component work.

This rule stays intentionally small so the reusable skill is the canonical source of current component standards, including `variants.ts`, `index.ts` exports, registry updates, mirrored demo components, interactive script lifecycle, and Base UI source review for comparable primitives.
